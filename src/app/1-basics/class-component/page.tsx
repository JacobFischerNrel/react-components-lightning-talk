'use client';

import React, { ReactNode } from 'react';

export default class BasicClassComponent extends React.Component {
  render(): ReactNode {
    return <div>This is a basic class-based component</div>;
  }
}
